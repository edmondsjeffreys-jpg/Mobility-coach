# Mobility Coach

Mobile-first PWA with Morning, Night, and Office routines, workout logging, breathing cues, and an AI Coach.

## Local
npm install
npm run dev

## Vercel
Import the GitHub repo into Vercel, then add `OPENAI_API_KEY` as an environment variable and redeploy.

Workout logs are stored in browser localStorage for v1. Cloud sync can be added later.
